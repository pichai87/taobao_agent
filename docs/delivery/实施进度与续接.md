# 原文架构补齐：实施进度与续接

日期：2026-09-23。总跟踪 Issue：<https://github.com/pichai87/taobao_agent/issues/1>。
分支：`feature/article-parity-20260923`。不包含 `docs/技术升级方案.md`；原有学习手册改动保持不动。

## 协作职责

- 主实现：业务接线、网页、集成测试、批次验收。
- 交付 Agent：Issue、分支、限定文件提交、draft PR、CI；不抢占业务代码。
- 审查 Agent：独立复现、反例测试、安全审查、修复建议与问题档案。
- 原文核对 Agent：逐项比对来源与真实实现，维护验收清单；实现独立归因与血缘模块。

## 已落地的本批代码（不等于全部原文完成）

1. V4 完整日期×品类视图，保留只有订单而缺少流量的数据；缺失 UV 保持未知，不伪造成 0。
2. 501 行哨兵检查：最多返回500行，超限明确失败，禁止静默截断总额。
3. Java 受控语义模型：SUM/RATIO 指标注册、日期与维度校验、PreparedStatement 参数化 SQL、JSqlParser AST 解析及可信编译结果比对。
4. 原有三条 Agent 链的 JdbcAnalyticsReader 已使用语义编译器，报告展示实际生成 SQL。模型仍不能自主选择任意指标/表/维度，不宣称通用 NL2MDL2SQL 已完成。
5. 网页语义工作台：五个指标、受控新指标注册、SQL/字段依赖预览、规则知识检索、个人摘要保存。
6. QueryAgent 获得模型、示例、口径、字段血缘文档、指标注册证据。仍是关系库关键词/过滤检索，不是已部署的向量 RAG 或 LightRAG。
7. GMV 诊断树：3因子 Shapley → 品类贡献 → 比率分子/分母，BigDecimal 金额守恒，明确深度、覆盖率、节点数、零分母与缺品类停止原因。
8. 网页 fetch SSE：认证头不进 URL、事件ID去重、任务切换取消旧连接、断流后保留游标重连。
9. Top N 数量裁剪；主报告仍限 GMV，遇明确非 GMV 请求拒绝错答，其他指标可在语义工作台查看。

## 验证流水（保留失败及其修复，不能只写最后一次通过）

- 第一次 Maven `test`：通过，包括原有链路及语义边界、读完整性测试。
- 前端原有 Node 测试：13/13 通过；后续新增 SSE parser 测试仍需纳入最终回归。
- 第二次 Maven `test`：发现既有 `vanishedCategoryStillHasNegativeContribution` 用例失败。原因是新增诊断模块要求当前期有行，而原金额计算允许当前期空集合表示全部品类消失。修复：保留原金额贡献计算，缺任一期间行时不生成诊断树；不会把缺数据伪造成完整诊断。最终回归待执行。
- 所有 Spring 集成测试使用独立 H2 内存库；未改写用户现有运行库。
- 模型集成回归使用脚本网关，不声称本次真实百炼模型已验收；没有读取用户 API Key，也没有发起付费请求。

### 第一批最终回归结果

- 第三轮发现 Top N 解析把测试协议 `TOP_N` 和普通单词内的 top 误认为排名数量；修复词边界，新增5个误匹配反例，不放宽真实数量校验。
- 2026-09-23 14:08（北京时间）`mvn -q -Dmaven.repo.local=D:\dvelop\Javaweb\.tools\m2 verify` 成功：bootstrap 158 项、eval 22 项，共 **180 项 Java 测试通过，0 失败、0 跳过**。包含真实1001条SSE回放、失败/取消事件写入故障的事务回滚。
- 最新 `node --test scripts/console.test.cjs`：**20/20 通过**，包含帧拆包/合包、超大单帧、错误取消底层连接、认证头不进入URL。
- 本机网页验收：独立18080端口/H2内存库，登录成功，离线GMV 2600/3000/-400、诊断树CVR贡献-400可见，语义工作台可预检并查询实际品类结果。没有访问/修改原有文件数据库。浏览器检查不等于付费大模型验收。
- 额度曾到93%；用户重置并要求继续，随后接口确认周窗口使用1%。因此继续第二批，不自动兑换任何卡。

## 问题如何追踪

每项问题的现象、最小复现、根因、影响、修复文件、验证证据、残余限制，见 `docs/reviews/审查记录.md`。
原文逐项验收和外部阻塞，见 `docs/delivery/原文逐项验收.md`。

## 未完成与续接顺序

1. 完成当前批测试、真实本机页面检查、问题回归，限定文件提交 draft PR；不合并未验收能力。
2. 独立 AST 字段血缘分析器，接入查询专家与网页；对不支持的 SQL 语法明确拒绝。
3. 完整结构化意图日期/指标/维度/次意图，以及模型选择语义计划的主链。
4. 指标注册公式恒等式校验与通用递归、多业务维度数据集、自动安全会话上下文。
5. 容器隔离 Python、MCP/worker 真实协议与可恢复执行、真实 Golden Case 指标与费用评测。
6. WrenAI/LightRAG 等开源组件按最终选择集成；淘宝内部 TisPlus3、DataWorks、ODPS/Hologres 需真实账号/服务权限与数据契约后验收，不以本地替代冒充接通。

## 额度与暂停原则

在可测试、可提交、可续接的位置检查额度。接近耗尽时记录最后成功命令、测试结果、未提交文件与下一步。不会自动兑换重置卡，也不会用“额度不足”掩盖普通实现未完成。
