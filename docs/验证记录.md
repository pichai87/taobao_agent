# 验证记录

日期：2026-09-14。本文各批记录按当时的验证范围保留；本轮开发验证没有调用付费模型，没有远程部署，没有修改全局 Java/Maven 配置。用户后来展示的单 Agent 真实模型轨迹不等于多 Agent 联调。

## 构建与自动测试

本机 Windows + JDK 25.0.4.1 + Maven 3.9.11。
编译目标 release 21。

最终执行：
`mvn -q -Dmaven.repo.local=工作区上级/.tools/m2 verify`

结果：退出码 0；39 项测试，失败 0、错误 0、跳过 0。

- AnalysisIntegrationTest：14 项，真实 Spring 上下文、H2、Flyway、MyBatis、StateGraph、MockMvc。
- RecoveryAndModelTest：3 项，持久检查点跳步恢复、取消状态保护、模拟模型返回枚举/非法内容。
- SafetyAndCalculationTest：22 项，包含 16 种不允许 SQL、空 SQL、白名单接受、
  零分母、消失品类、无对比数据不伪造贡献、离线意图。

这些是工程测试，**不是 39 条真实模型 Golden Case，也不是安全性的数学证明**。
当前只有 H2 数据库实测，不以 H2 通过推断 MySQL 权限一定正确。

可运行产物：
ecom-agent-bootstrap/target/ecom-agent-bootstrap-0.1.0-SNAPSHOT.jar

测试原始结果：
各模块 target/surefire-reports。target 不提交到 Git，重新 verify 会生成。

## 真实 HTTP 冒烟

将已打包 JAR 监听在 127.0.0.1:18080，默认 local 配置。
首次沙箱启动因文件权限阻止 H2 建目录失败；经执行权限审批后正常启动。
这是工具运行环境限制，不通过删除数据库或修改系统 ACL 绕过。

实际通过：
1. Flyway 在文件数据库执行 V1/V2。
2. scripts/smoke.ps1 获取认证 + 会话 + CSRF，提交归因任务。
3. 任务 SUCCEEDED：目标 GMV 2600、对比 3000、家电贡献 -400。
4. SSE 返回六节点的事件与最终 SUCCEEDED。
5. Last-Event-ID=7 重连，从 id=8 开始，不重复 id=7。
6. health=UP；未认证 GET /api/runs 返回 401。
7. PowerShell 对 build/start/smoke 三个脚本语法解析通过。

最后两项公式/工具权限边界修改后已重新 verify、重新生成 JAR。
HTTP 冒烟针对该次修改前的打包版本；修改涉及路径有自动测试覆盖。
验收服务已停止，18080 没有继续监听；合成数据与任务审计保留在 var 目录。

## 尚未执行的验证

JDK 21 实机运行、Docker 镜像构建、MySQL 权限测试、远程 CI、
真实模型接口、浏览器 SSE、慢客户端 SSE、并发压力、进程崩溃注入、
依赖漏洞扫描和完整渗透测试。

JDK 25 上出现第三方 Unsafe / Mockito 动态 Agent 警告；本次不影响测试通过，
但不能把警告解释为已经完成未来 JDK 兼容验证。

## 2026-09-14 补充：本机网页控制台

本节补充上述历史记录。沿用现有文件数据库，未删除旧任务和审计数据；未使用模型密钥。

自动检查：

- 最终 `mvn verify` 退出码 0：新增公开页面与 API 认证边界测试。Java 测试总数 40（15 + 3 + 22），失败、错误、跳过均为 0。
- `node --check ecom-agent-bootstrap/src/main/resources/static/app.js`：语法通过。
- `node --test scripts/console.test.cjs`：7 项通过。模拟 DOM/API 测试，不能替代浏览器或真实后端测试。
- 前端回归检查错误登录、金额渲染、纯文本安全、定义空数据、确认后串行轮询、失败和网络重试、幂等键复用。
- GitHub CI 增加 Node 逻辑测试步骤；没有把配置已写入视为远程 CI 已运行。

真实浏览器在本机 8080 上验证：

1. 未登录打开页面成功；错误密码显示中文提示，不出现原生认证错误页。
2. 正确登录后表单隐藏，健康检查通过，明确显示离线规则模式。
3. 默认归因任务完成：2600 / 3000 / -400 / -13.33%，家电 -400，六节点事件与报告可见。
4. 勾选确认后先等待；确认后自动读取最终报告，无需手动刷新。
5. 待确认任务取消成功，未执行查询节点。
6. 定义问题不显示金额卡片，明确没有查询业务表。
7. 日期 2000-01-01 返回 NO_DATA，页面没有旧报告冒充成功结果。
8. 七日趋势显示 21 条明细；SQL 与四条口径证据可展开查看。
9. 退出回到登录页，页面状态清除。
10. 最终 JAR 重启后重新登录，历史任务保留；选择旧归因任务仍显示正确金额及中文报告标题。

最终版本已在本机后台启动，监听 127.0.0.1:8080；未注册为开机服务。
本次启动日志在 `var/console-final-20260914.stdout.log` 和对应 stderr.log，未纳入 Git。

旧错误页标签在自动化刷新时受到浏览器工具 URL 策略限制；新建普通 HTTP 标签访问成功。
没有更改浏览器安全策略、系统代理或关闭项目认证。
检查了页面实际截图；全页截图出现拼接重复，而 DOM 中事件和明细各只有一份。

未覆盖：浏览器进程崩溃后的恢复按钮端到端验证、真实移动设备、多浏览器矩阵、弱网压力测试。
当前 UI 用 HTTP 轮询，不计为 SSE 浏览器验收，也不计为 Vue/ECharts 实施完成。

## 2026-09-14 补充：百炼工具循环与需求验收表

自动检查在 JDK 25 上执行，编译目标 Java 21：

- `mvn verify` 成功；64 个 Java 测试，失败/错误/跳过均为 0。组成：原 40 + 模型协议 4 + 配置接口 3 + 工具循环 12 + 需求接口 5。
- Node 前端逻辑回归 11 项通过，覆盖配置只传会话 ID、清空 Key 输入、独立模型文本、验收保存和版本冲突保留本地备注。
- 工具循环测试使用模拟供应商决策，但查询真实 H2 测试库：确认多轮工具结果回传、金额计算、未知工具拒绝、参数校验、证据门槛、对比完整性、重复调用 ID、轮次/工具/token 限制和清除/取消。
- 需求接口测试覆盖 28 项目录、认证/CSRF、按账号隔离、版本冲突、审计记录、无效完成度和疑似 Key 备注拒绝。
- Flyway V3 真实迁移现有文件数据库成功，保留旧任务，不删除/重建 var/agent。

本机浏览器实际验证：

1. 新版页面登录成功，模型配置、模式选择和验收表可见。
2. 用明确的测试占位值保存配置：输入框随后清空，显示过期时间和官方域名，提示保存不等于连通验证。
3. 模型任务 `6b464f75-dab3-4689-891b-3297e3f6f91a` 勾选确认后处于等待状态，无工具事件；没有批准有效配置下的执行。
4. 清除配置后批准同一任务，收到 MODEL_SESSION_EXPIRED；再取消任务成功。没有向外部供应商发请求。
5. 验收表加载 28 项，修改验证级别、备注、返工并逐行保存，返回版本 1。刷新表格后备注及返工标记保留；返工过滤生效。
6. 离线回归任务 `5750b8d6-46da-4413-b965-36357a64f589` 成功，金额 2600 / 3000，差额 -400，变化率 -13.33%；六节点轨迹和真实明细均可见。
7. 检查网页截图：窄屏表格支持横向滚动，备注为可编辑文本；不是图片或静态假勾选。

本次未提供真实 Key，所以没有真实百炼端到端、模型准确率/成本测量或生产容量验证。不能把 mock（模拟响应）通过当作外部模型可用。
目录默认验证程度与个人数据库记录分开；数据库备注不提交 Git。真实模型验收由用户自愿配置并确认付费执行后补充。

## 多 Agent 协作补齐（2026-09-14 后续批次）

- Maven verify 成功：88 项 Java 测试，失败、错误、跳过均为 0；Node 前端逻辑回归 13 项通过。
- Java 组成：分析 15、协议 4、模型配置 3、单 Agent 循环 12、多 Agent API 2、多 Agent 集成 21、固定对照 1、恢复 3、需求 5、计算与安全 22。
- 多 Agent 集成覆盖八条意图路线、按专家限制工具、独立上下文、补充缺失对比数据、证据包任务/日期/范围校验、错误路由拒绝、低置信度回退、认证错误传播、取消/清除、全局轮数/工具/token/派发预算，以及单/多模式共享执行锁。
- 调用 ID 按独立对话隔离：同一对话内重复拒绝；不同专家对话可使用相同供应商 ID。
- Ops 状态工具只返回当前账号的任务元数据。没有测试或开放服务器重启/删库等运维写操作。
- AgentComparisonTest 使用固定供应商决策 + 真实 H2 工作流，六类结构化结果一致。对比任务 4→7 次模型请求，最大可见工具 6→2；查询原始对话不进入分析专家输入。
- 测试原始输出已归档至 [agent-comparison-20260914.json](verification/agent-comparison-20260914.json)。请求字节数不是 token，不据此推算账单。没有完成真实模型准确率、成本、耗时 A/B，也没有证明优势全部来自多 Agent 拓扑。
- 核对旧服务 PID 34040 的精确项目路径且无运行中任务后，重启到新 JAR（PID 46764）。沿用原 H2 文件，未删除数据；临时 Key 因正常重启失效。
- 实际浏览器：登录正常，新执行方式含多 Agent 与单 Agent 对照；无配置时禁用模型选项。只保存明确无效的测试凭据，不发起供应商请求。
- 网页待确认任务 6a5a5260-008d-43ec-b5c6-654d77ce6f5d：模式为 LLM_MULTI_AGENT，尚无模型/交接事件，面板明确显示不能据此认定协作成功。随后取消任务并清除测试配置。
- 升级后网页离线回归任务 de8861ac-1880-46cf-bb02-e08e80f62e6d 成功：2600 / 3000、差额 -400、-13.33%，真实六步事件及查询明细可见。历史任务保留；没有把这次离线结果标为多 Agent 成功。

详见 [多Agent协作与对照验证](多Agent协作与对照验证.md)。运行 88 个 Java 测试不是做了 88 次真实模型评测；浏览器待确认验收也不是多 Agent 真实供应商端到端验收。

最终重启检查：原模型配置测试值已清除；新版进程重启并重新登录后，两个验收项的版本 1 备注仍存在，返工标记保留。随后更新验收表自身的本机验证结果，解除测试用返工标记。
最终服务仅在本机 8080 后台运行，日志为 `var/model-board-final-20260914.stdout.log` 及对应 stderr.log，均忽略提交；未设置开机启动。
环境审批短暂容量不足曾导致一次启动没有执行；重新确认目标后重试成功，不涉及关闭认证或修改安全策略。
