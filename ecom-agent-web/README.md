# Vue 分析控制台（待实现）

按既定计划保留 Vue 3 + ECharts 控制台目录。
现有可用界面位于 `../ecom-agent-bootstrap/src/main/resources/static/`，
使用 HTML/CSS/JavaScript，与后端一起打包，连接真实 API；此目录没有伪造的 Vue 工程。

后续界面：身份验证、问题与明确日期输入、运行列表、审批、SSE 节点追踪、
GMV 图表、SQL 模板、证据、失败原因与恢复按钮。

对接契约见 ../docs/接口与演示.md。
密码不得写入前端源码或 localStorage；所有接口必须使用后端返回的真实数据。
现在可在 http://127.0.0.1:8080/ 操作真实分析，也保留 scripts/smoke.ps1。
这次为解决本机登录和可视化验证问题增加内置界面，不改变后端架构或原企业计划。
# 网页模型配置与需求验收

当前静态网页已增加百炼临时 Key、模型工具调用执行方式及 28 项可保存验收清单。页面与 Java 后端一起打包，说明见 `docs/模型工具调用与需求验收.md`。
这不是完整 Vue/ECharts 控制台，也不表示原文多专家系统全部实现。
